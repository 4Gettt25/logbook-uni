# Copy this file to `.env` and adjust values locally.

# Activate postgres profile (default uses H2)
SPRING_PROFILES_ACTIVE=postgres

# Database connection (used when postgres profile active)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=logbook
DB_USER=postgres
DB_PASSWORD=<your_password>

# Optional: override server port
# SERVER_PORT=8080
